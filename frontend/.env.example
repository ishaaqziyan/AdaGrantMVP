# Base URL of the offchain API server (../offchain).
PUBLIC_API_BASE_URL=http://localhost:3000

# Explorer tx-lookup base URL; the tx hash is appended directly. Must match
# the network offchain/.env's BLOCKFROST_PROJECT_ID points at.
PUBLIC_EXPLORER_TX_BASE_URL=https://preview.cardanoscan.io/transaction
